<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsss.ankush.shlokaabhyasanupdated">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SubCategoryActivity"
            android:label="@string/title_activity_sub_category" />
        <activity
            android:name=".HymnsActivity"
            android:label="@string/title_activity_hymns" />
        <activity
            android:name=".HymnShow"
            android:label="@string/title_activity_hymn_show" />
        <activity android:name=".DownloadsActivity" />
        <activity android:name=".DownloadShowActivity" />
        <activity android:name=".TrackedActivity" />
        <activity android:name=".TrackedShowActivity" />
        <activity android:name=".FavouritesActivity" />
        <activity android:name=".MeaningsHymnActivity"></activity>
    </application>

</manifest>